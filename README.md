# fibos-claimer

use Docker Compose
```
cp .env.sample .env
docker-compose up -d
```

Or to install dependencies:

```bash
curl -fsSL https://bun.sh/install | bash
bun install
cp .env.sample .env
```

To run:

```bash
bun do
```
